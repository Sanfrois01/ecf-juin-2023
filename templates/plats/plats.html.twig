{% extends 'base.html.twig' %}

{% block title %}Les Plats
{% endblock %}

{% block body %}

{% for message in app.flashes('success') %} 

<div class=" alert alert-success d-flex justify-content-center">
    {{message}}
</div>

{% endfor %}


		<h1 class="text-center py-5 mx-5">Les Plats</h1>
		<h2 class="text-center">Voici les plats que nous proposons</h2>



<div class="container js-filter content">
	<div class="d-md-flex ">
				<div class "col-md-10 ">
					{% include 'plats/_filter.html.twig' with {form:form} only %}
				</div>

			<div class='d-md-flex row mx-3 py-3 js-filter-content'>
			
			{% include 'plats/_plats.html.twig' %}

				<div class="d-flex justify-content-center">{{ knp_pagination_render(plats) }}</div>
			</div>
</div>

{% if is_granted('ROLE_ADMIN') %}

<div class="d-flex row mx-3 py-3 justify-content-center ">
	<button type="button" class="btn btn-primary">
		<a class="nav-link" href="{{path('plats_create')}}"> Creer un plat!
	</button>

</div>
{% else %}


{% include "accueil/_reservationButton.html.twig" %}

{% endif %}



	<body>

	</body>
</div></body>{% endblock %}
